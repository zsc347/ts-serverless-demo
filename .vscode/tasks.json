{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "npm",
            "script": "watch",
            "group": "build",
            "label": "npm: watch",
            "detail": "tsc -b tsconfig.build.json -w"
        }
    ]
}
